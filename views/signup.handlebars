{{!-- 
<div class="flex-wrap">
    <fieldset>
        <form method="POST" action="/signup">            
            <br>
            <input type="radio" name="rg" id="sign-in" />
            <input type="radio" name="rg" id="sign-up" checked/>
            <input type="radio" name="rg" id="reset" />      
            
            <label for="sign-in">Sign in</label>
            <label for="sign-up">Sign up</label>
            <label for="reset">Reset</label>  
    
            <input class="sign-up sign-in"   type="text"  name="name" id="sign-up-sign-in"  placeholder="Name" value={{this.name}}>
            <input class="sign-up sign-in reset"   type="email" name="email" id="sign-up-sign-in"  placeholder="Email" value={{this.email}}>
            <input class="sign-up sign-in" type="password" name="password" id="sign-up-sign-in" placeholder ="Password">
            <input class="sign-up" name="repassword" id="sign-up-sign-in" type="password" placeholder ="Repeat Password" >
            <button class="form-button" id="sign-up-sign-in" type="submit"></button>        

            <br>
          
            <br>
        </form>
        {{#each message}}
            <div class="errors">{{this}}</div>
        {{/each}}
    </fieldset>
  
</div> --}}

<div class="containerform">
  <div class="form-part">
    <h2>Simple Form Design</h2>
<form method="POST" action="/home">

        <div class="text-input">
            {{#each message}}
            <div class="errors">{{this}}</div>
            {{/each}}
          <br>
   
               <div class="text-input">
        <label for="name">Full Name</label>
        <input type="text" name="name" id="name">
      </div>


            <div class="text-input">
              <label for="email">Email</label>
              <input type="email" name="email" id="email">
            </div>
         
        </div>       <div class="text-input">
        <label for="password">Password</label>
        <input type="password" name="password" id="password">
      </div>
           <div class="text-input">
              <label for="Re-Password">Re-Password</label>
              <input type="password" name="repassword" id="repassword">
            </div>
            <div class="row align-items-center justify-content-center">
        <button class="btn btn-secondary btn-sm" value="Submit" type="submit">Sign up</button>        
            </div>
    
     
    </div>
    </form>
  </div>
</div>